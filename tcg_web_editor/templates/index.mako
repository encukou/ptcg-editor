<%inherit file="base.mako" />

<div class="container">

    <h1>Pokébeach Card Database Editor</h1>

    <p>Currently, the <a href="${h.url('/sets')}">set list</a> has ${len(sets)} sets.</p>

</div>
