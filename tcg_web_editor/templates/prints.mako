<%inherit file="base.mako" />

<div class="container">

    <h1>Prints</h1>

    <p>
        For now, please use the the
            <a href="${this.root['sets'].url}">set list</a>
        or the
            <a href="${this.root['families'].url}">card families</a>
        to get at the cards.</p>

</div>
